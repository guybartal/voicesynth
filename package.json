{
  "name": "voicesynth",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "A local Azure Speech text-to-speech studio.",
  "engines": {
    "node": ">=22.9.0"
  },
  "scripts": {
    "start": "node --env-file-if-exists=.env server.mjs",
    "mcp": "node mcp.mjs",
    "dev": "node --watch --env-file-if-exists=.env server.mjs",
    "test": "node --test"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.30.0",
    "zod": "^4.6.5"
  }
}
